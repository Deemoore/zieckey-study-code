该聊天程序的最简单版本，甚至可以说不是一个聊天程序

程序功能：
服务器对每个连接的客户端发送一个连接成功的消息，然后等待下一个连接到来。
	socket-->bind-->listen-->accept
客户端连接服务器后，接收服务器发来的消息，然后退出。
	socket-->connect-->read
	
使用方法：
./server
服务器启动程序，后带参数为服务器绑定端口，一半设置为大于1024的非知名端口。
例如  ./server 1234
	
./client
客户端程序，后带参数为服务器IP地址和绑定端口
登录到服务器后，接收服务器消息。
